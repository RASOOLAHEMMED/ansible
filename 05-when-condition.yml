- name: Demo on when condition
  hosts: localhost
  vars:
    URL: google.com
  tasks:
   - name: condition on strings
     when: URL == "google.com"
     ansible.builtin.debug:
     msg: Hello string